# GitLab_AWS

## Purpose and Intent

To build a public portfolio using GitLab instead of GitHub. Reason being of having more control over the data and actually having to build it versus just using a service that I don't own.
